import User from "../models/schemaName.js";

export const function_name = async (req, res, next) => {
  try {
  } catch (error) {
    res.status().json({ success: false, message: error.message });
  }
};
